#!/bin/bash

SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
PROJECT_DIR="$(dirname $SCRIPT_DIR)"

USERNAME=$1
SCRIPT_NAME=$(basename $2)

docker-compose run python-worker /usr/src/app/register.py $USERNAME $SCRIPT_NAME